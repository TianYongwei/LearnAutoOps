# 更换yum源，换成国内的阿里源或者网易源

# 安装必要软件

# 环境变量配置

# 非root用户下的环境变量控制，在~/.bash_profile 中添加环境变量，在~/.bashrc 最后一行添加 source ~/.bash_profile
# .bashrc 是启动bash启动就会执行的文件(又看了一篇文章)

# ~/.bash_profile 远程登陆主机的时候会执行这个文件
# ~/.bashrc 虚拟机内部打开命令行窗口时会执行这个文件
# 通常二者设置大致相同，所以通常前者会调用后者

# 脚本！！想要自动化就写脚本！！